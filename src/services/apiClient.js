// Archivo puente para compatibilidad con código antiguo.
// Reexporta la instancia principal definida en `api.js`.

export { API_BASE_URL, tokenManager } from './api';
export { default } from './api';